# Copy the values below into a `.env` file in the project root.
# .env is git-ignored — never commit real credentials.

# Required by tests/admin-user-mgmt.spec.ts
ORANGEHRM_BASE_URL=https://opensource-demo.orangehrmlive.com
ORANGEHRM_USERNAME=Admin
ORANGEHRM_PASSWORD=admin123

# Optional — used by tests/admin-user-mgmt.spec.ts for the non-admin permission test.
# Defaults to the values below if not set.
NON_ADMIN_USERNAME=fiona
NON_ADMIN_PASSWORD=OfficePassword@123
EOF